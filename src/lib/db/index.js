/**
 * DATABASE STUB
 * ─────────────────────────────────────────────
 * Replace this with your ORM of choice.
 *
 * Recommended:
 *   - Prisma + PostgreSQL (self-hosted / Supabase)
 *   - Drizzle + Neon (serverless Postgres)
 *
 * When ready:
 * 1. Install your ORM
 * 2. Define schema in prisma/schema.prisma or lib/db/schema.js
 * 3. Replace the stub exports below with real client instances
 * 4. Connect user progress, submissions, and quiz results
 */

export const db = null;

export default function PricingPage() {
  return (
    <section className="panel p-6">
      <h1 className="text-3xl font-bold font-[family-name:var(--font-display)]">Pricing</h1>
      <p className="mt-3 text-sm text-[var(--text-muted)]">
        Pricing plans are a placeholder in this iteration. Core practice features are currently accessible without a paywall.
      </p>
    </section>
  );
}
